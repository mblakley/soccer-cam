# Server worker config — DESKTOP-5L867J8
# Uses local API (same process), local file paths.

[worker]
hostname = "DESKTOP-5L867J8"
capabilities = ["stage", "tile"]
api_url = "http://127.0.0.1:8643"
server_share = ""
local_work_dir = "G:/pipeline_work"
local_models_dir = "D:/training_data/models"

[resources]
max_gpu_temp = 85
min_disk_free_gb = 25
gpu_device = 0
idle_games = []

[heartbeat]
interval = 30
