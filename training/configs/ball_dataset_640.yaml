# YOLO dataset config for ball detection training (640x640 tiles)
# Usage: yolo train data=training/configs/ball_dataset_640.yaml model=yolo11m.pt

path: F:\training_data\ball_dataset_640
train: train.txt
val: val.txt

nc: 1
names: ["ball"]
