# YOLO dataset config for ball detection training
# Usage: yolo train data=training/configs/ball_dataset.yaml model=yolo11n.pt

path: training/data/ball_dataset  # relative to project root
train: images/train
val: images/val
test: images/test

nc: 1
names: ["ball"]
